<Window x:Class="Pizza.MVVM.View.ManagerChild.FormBestWorseReportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pizza.MVVM.View.ManagerChild"
        mc:Ignorable="d"
        xmlns:viewModel="clr-namespace:Pizza.MVVM.ViewModel.ManagerChild"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="FormBestWorseReportView" Height="800" Width="1000">
    <Window.DataContext>
        <viewModel:FormBestWorseReportViewModel/>
    </Window.DataContext>

    <Grid>
        <DataGrid AutoGenerateColumns="False"
                  ItemsSource="{Binding DiagramData}"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Key}" />
                <DataGridTextColumn Header="Количество" Binding="{Binding Value}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
